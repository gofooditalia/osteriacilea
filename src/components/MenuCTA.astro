<section
    id="prenota"
    class="min-h-screen flex items-center py-20 bg-cover bg-center bg-no-repeat relative overflow-hidden isolate"
>
    <!-- Slideshow for Mobile -->
    <div class="slideshow-container absolute inset-0 z-0 md:hidden">
        <div class="slide bg-[url('/images/cilea-bg-booking-smartphone.png')]">
        </div>
        <div
            class="slide bg-[url('/images/cilea-bg-booking-smartphone-002.png')]"
        >
        </div>
        <div
            class="slide bg-[url('/images/cilea-bg-booking-smartphone-003.png')]"
        >
        </div>
    </div>

    <!-- Overlay for better readability -->
    <div class="absolute inset-0 bg-neutral-900/40 dark:bg-neutral-900/60 z-10">
    </div>

    <div class="max-w-[1200px] mx-auto px-6 lg:px-10 w-full relative z-20">
        <div
            class="bg-white/10 dark:bg-black/20 backdrop-blur-md border border-white/20 rounded-2xl p-8 md:p-16 flex flex-col md:flex-row items-center justify-between gap-10 shadow-2xl"
        >
            <div class="max-w-md space-y-4">
                <h2
                    class="text-4xl md:text-5xl font-bold text-white font-display italic drop-shadow-md"
                >
                    Assicura il tuo posto a tavola
                </h2>
                <p class="text-white/90 text-lg drop-shadow-sm">
                    Organizziamo cene di famiglia, eventi speciali o semplici
                    serate tra amici. Prenota online per garantirti la migliore
                    esperienza.
                </p>
            </div>
            <div class="flex flex-col gap-4 w-full md:w-auto">
                <a
                    href="https://www.google.com/maps/reserve/v/dine/c/CG8eycUaKzk?source=pa&opi=89978449&hl=it-IT&gei=W5SAabqONIPv7_UP8viT6AI&sourceurl=https://www.google.com/search?q%3Dosteria%2Bcilea%26oq%3Dosteria%26pf%3Dcs%26sourceid%3Dchrome%26ie%3DUTF-8"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="bg-primary text-white px-10 py-5 rounded-xl font-bold text-xl shadow-lg hover:shadow-primary/40 hover:-translate-y-1 transition-all flex items-center justify-center gap-3 active:scale-95"
                >
                    <span class="material-symbols-outlined">event</span>
                    Prenota Ora
                </a>
                <p class="text-xs text-center text-white/80">
                    Oppure chiamaci: <span class="font-bold text-white"
                        >+39 392 592 2651</span
                    >
                </p>
            </div>
        </div>
    </div>
</section>

<style>
    /* Stacking Context */
    #prenota {
        background-color: transparent;
    }

    .slide {
        position: absolute;
        inset: 0;
        background-size: cover;
        background-position: center;
        opacity: 0;
        animation: crossfade 12s infinite ease-in-out;
    }

    .slide:nth-child(1) {
        animation-delay: 0s;
    }
    .slide:nth-child(2) {
        animation-delay: 4s;
    }
    .slide:nth-child(3) {
        animation-delay: 8s;
    }

    @keyframes crossfade {
        0% {
            opacity: 0;
        }
        10% {
            opacity: 1;
        }
        33% {
            opacity: 1;
        }
        43% {
            opacity: 0;
        }
        100% {
            opacity: 0;
        }
    }

    /* Desktop Background (Static) */
    @media (min-width: 768px) {
        #prenota {
            background-image: url("/images/cilea-bg-booking-desktop.png");
            background-size: cover;
            background-position: center;
        }
        .slideshow-container {
            display: none;
        }
    }
</style>
